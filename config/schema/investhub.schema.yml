# Schema for the theme setting configuration file of the THEMETITLE theme.

cds_wxt.settings:
  type: theme_settings
  label: 'CDS WXT settings'
  mapping:
    cdn_provider:
      type: string
      label: 'CDN Provider'
    breadcrumb_home:
      type: integer
      label: 'Breadcrumb Home'
